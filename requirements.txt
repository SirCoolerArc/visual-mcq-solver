# Python 3.12 + NVIDIA GPU (CUDA 12.x driver, using cu124 wheels)
# Install: pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu124

torch>=2.5.0,<2.7.0
torchvision>=0.20.0

transformers>=4.49.0
accelerate>=1.0.0
safetensors>=0.4.5
bitsandbytes>=0.43.0
qwen-vl-utils>=0.0.14

Pillow>=10.0.0
pandas>=2.2.0
tqdm>=4.65.0
